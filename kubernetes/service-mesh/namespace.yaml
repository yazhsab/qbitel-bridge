apiVersion: v1
kind: Namespace
metadata:
  name: qbitel-service-mesh
  labels:
    name: qbitel-service-mesh
    app.kubernetes.io/name: qbitel
    app.kubernetes.io/component: service-mesh
    app.kubernetes.io/part-of: qbitel-platform
    qbitel.ai/quantum-safe: "true"
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: qbitel-service-mesh-quota
  namespace: qbitel-service-mesh
spec:
  hard:
    requests.cpu: "20"
    requests.memory: 40Gi
    limits.cpu: "40"
    limits.memory: 80Gi
    persistentvolumeclaims: "10"
    services.loadbalancers: "5"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: qbitel-service-mesh-limits
  namespace: qbitel-service-mesh
spec:
  limits:
  - max:
      cpu: "4"
      memory: 8Gi
    min:
      cpu: "100m"
      memory: 128Mi
    default:
      cpu: "500m"
      memory: 512Mi
    defaultRequest:
      cpu: "250m"
      memory: 256Mi
    type: Container
  - max:
      cpu: "8"
      memory: 16Gi
    min:
      cpu: "200m"
      memory: 256Mi
    type: Pod
