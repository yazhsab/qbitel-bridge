# CRONOS AI - Chaos Engineering Tests
# 
# Chaos Mesh experiments to test system resilience and fault tolerance.

---
apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: cronos-ai-pod-failure
  namespace: cronos-ai
spec:
  action: pod-failure
  mode: one
  duration: "30s"
  selector:
    namespaces:
      - cronos-ai
    labelSelectors:
      app: cronos-ai-engine
  scheduler:
    cron: "@every 1h"

---
apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: cronos-ai-network-delay
  namespace: cronos-ai
spec:
  action: delay
  mode: one
  selector:
    namespaces:
      - cronos-ai
    labelSelectors:
      app: cronos-ai-engine
  delay:
    latency: "100ms"
    correlation: "25"
    jitter: "10ms"
  duration: "2m"
  scheduler:
    cron: "@every 2h"

---
apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: cronos-ai-network-partition
  namespace: cronos-ai
spec:
  action: partition
  mode: one
  selector:
    namespaces:
      - cronos-ai
    labelSelectors:
      app: cronos-ai-engine
  direction: both
  duration: "1m"
  scheduler:
    cron: "@every 4h"

---
apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: cronos-ai-cpu-stress
  namespace: cronos-ai
spec:
  mode: one
  selector:
    namespaces:
      - cronos-ai
    labelSelectors:
      app: cronos-ai-engine
  stressors:
    cpu:
      workers: 2
      load: 80
  duration: "3m"
  scheduler:
    cron: "@every 6h"

---
apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: cronos-ai-memory-stress
  namespace: cronos-ai
spec:
  mode: one
  selector:
    namespaces:
      - cronos-ai
    labelSelectors:
      app: cronos-ai-engine
  stressors:
    memory:
      workers: 1
      size: "512MB"
  duration: "2m"
  scheduler:
    cron: "@every 6h"

---
apiVersion: chaos-mesh.org/v1alpha1
kind: IOChaos
metadata:
  name: cronos-ai-io-delay
  namespace: cronos-ai
spec:
  action: latency
  mode: one
  selector:
    namespaces:
      - cronos-ai
    labelSelectors:
      app: cronos-ai-engine
  volumePath: /data
  path: "/**/*"
  delay: "100ms"
  percent: 50
  duration: "2m"
  scheduler:
    cron: "@every 8h"

---
apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: cronos-ai-pod-kill
  namespace: cronos-ai
spec:
  action: pod-kill
  mode: fixed
  value: "1"
  selector:
    namespaces:
      - cronos-ai
    labelSelectors:
      app: cronos-ai-engine
  scheduler:
    cron: "@every 12h"

---
apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: cronos-ai-packet-loss
  namespace: cronos-ai
spec:
  action: loss
  mode: one
  selector:
    namespaces:
      - cronos-ai
    labelSelectors:
      app: cronos-ai-engine
  loss:
    loss: "10"
    correlation: "25"
  duration: "2m"
  scheduler:
    cron: "@every 4h"

---
# Workflow to run multiple chaos experiments in sequence
apiVersion: chaos-mesh.org/v1alpha1
kind: Workflow
metadata:
  name: cronos-ai-chaos-workflow
  namespace: cronos-ai
spec:
  entry: entry
  templates:
    - name: entry
      templateType: Serial
      deadline: 30m
      children:
        - network-delay-test
        - cpu-stress-test
        - memory-stress-test
        - pod-failure-test
    
    - name: network-delay-test
      templateType: NetworkChaos
      deadline: 5m
      networkChaos:
        action: delay
        mode: one
        selector:
          namespaces:
            - cronos-ai
          labelSelectors:
            app: cronos-ai-engine
        delay:
          latency: "200ms"
        duration: "3m"
    
    - name: cpu-stress-test
      templateType: StressChaos
      deadline: 5m
      stressChaos:
        mode: one
        selector:
          namespaces:
            - cronos-ai
          labelSelectors:
            app: cronos-ai-engine
        stressors:
          cpu:
            workers: 2
            load: 90
        duration: "3m"
    
    - name: memory-stress-test
      templateType: StressChaos
      deadline: 5m
      stressChaos:
        mode: one
        selector:
          namespaces:
            - cronos-ai
          labelSelectors:
            app: cronos-ai-engine
        stressors:
          memory:
            workers: 1
            size: "1GB"
        duration: "3m"
    
    - name: pod-failure-test
      templateType: PodChaos
      deadline: 5m
      podChaos:
        action: pod-failure
        mode: one
        selector:
          namespaces:
            - cronos-ai
          labelSelectors:
            app: cronos-ai-engine
        duration: "2m"